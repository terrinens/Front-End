// 무명 함수
var printMsg = function(msg) {
    document.write ("무명함수 : " + msg + "<br/>");
}

// 기본 함수
function printMsg (msg) {
    document.write ("기본 함수 : " + msg + "<br/>");
}

// 함수 호출
printMsg("호출했습니다.");
