var city = ["Seoul", "Pushan", "Incheon"]

// 함수 호출시 배열에 저장된 값을 출력
function printArr() {
    for (var i = 0; i < city.length; i++) {
        document.write("배열에 저장된 데이터 : " + city[i] + "<br />");
    }
}

printArr();

document.write("<hr>")
